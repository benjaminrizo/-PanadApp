import { useState } from "react";
import { useNavigate } from "react-router-dom";

export default function ClientProfile() {
  return <div className="p-8 text-on-surface font-sans">Perfil del Cliente — próximamente</div>;
}